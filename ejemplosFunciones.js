//Creo una función declarativa con el nombre "saludo" cuyo parametro es "estudiante"
//una variable sin valor definido, pues se le da después.
function saludo (estudiante){
    console.log(estudiante);//Función para imprimir cosas en la consola del navegador
}
//Se llama a la función y se le da valor a la variable "estudiante"
saludo("Lucy");

//Al correrse en la terminal del navegador, se muestra el valor dado a "estudiante"