//En terminal
Boolean();
//>false por default

//Valores falsos
Boolean(0);
Boolean(undefined);
Boolean(false);
Boolean("");//vacio

//Verdaderos verdaderos
Boolean(" ");//con un caracter
Boolean("a");//caracteres
Boolean(1); //cualquier numero salvo 0
Boolean([]);//arrays true por default aunque este vacio
Boolean({});//objeto true por default ""
Boolean(function(){});//true por default ""
Boolean(true);